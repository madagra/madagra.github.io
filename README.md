# Mario Dagrada website

Personal portfolio built with Next.js, TypeScript, and React.

## Develop locally

```sh
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000).

## Production check

```sh
pnpm build
pnpm lint
```

The legacy Jekyll source is retained in `_archive/jekyll/`.
